server:
  port: 8080

spring:
  datasource:
    username: mydb
    password: dSHZKqbktVxIDil
    url: jdbc:mysql://localhost:3306/salary_accounting?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        jdbc:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true